# files to be removed from rootfs
#
# format: package_name/path/to/remove

# executables that are not required (di-live includes dummy replacements)
di-utils/bin/anna-install
di-utils/bin/apt-install

# replaced by di-live python script which logs to journal
di-utils/bin/log-output

# debconf already has debconf-set-selections ("proper" debconf installed)
preseed-common/bin/debconf-set-selections

# broken symlink to dumpe2fs in udeb
# (dumpe2fs is only in full e2fsprogs package)
e2fsprogs-udeb/usr/sbin/e2mmpstatus

# NOTE: a number of libs in e2fsprogs-udeb conflict with those provided by
# other packages on base system. Those already installed by default are
# blacklisted here. Other lib conflicts with packages which are not installed
# by default in TKL base system are handled as package conflicts. See
# 'Conflicts'/'Breaks' sections in debian/control.

# conflict with lib from libcom-err2 pkg
e2fsprogs-udeb/usr/lib/x86_64-linux-gnu/libcom_err.so.2
e2fsprogs-udeb/usr/lib/x86_64-linux-gnu/libcom_err.so.2.1

# conflict with lib from libss2 pkg
e2fsprogs-udeb/usr/lib/x86_64-linux-gnu/libss.so.2
e2fsprogs-udeb/usr/lib/x86_64-linux-gnu/libss.so.2.0

# uneeded lintian override
e2fsprogs-udeb/usr/share/lintian/overrides/e2fsprogs-udeb

# config files that we don't want to overwrite
rootskel/init
rootskel/etc/fstab
rootskel/etc/group
rootskel/etc/inittab
rootskel/etc/modules.conf
rootskel/etc/mtab
rootskel/etc/nsswitch.conf
rootskel/etc/passwd
rootskel/etc/profile

# unneeded binaries
rootskel/sbin/init
rootskel/sbin/shutdown
